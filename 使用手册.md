# spamguard-bot-v3 部署使用手册

这是一份从零到跑起来的部署使用说明，适用于 1C1G 小内存 VPS。手册里所有 token、ID、密钥、域名都是占位符，请替换成你自己的真实值。真实值只写在 VPS 本机的 `.env` 里，永远不要提交到 GitHub。

## 一、准备工作

开始前你需要准备好这几样东西：

- 一台能连外网的 Linux VPS（1C1G 即可），有 root 权限。
- 一个 Telegram 机器人 Token：找 `@BotFather` 创建机器人后获得，格式类似 `123456789:AAxxxxxxxxxxxxxxxx`。
- 你自己的 Telegram 数字 ID：找 `@userinfobot` 发一条消息就能拿到，是一串纯数字。

## 二、安装 Docker

VPS 上依次执行，安装 Docker 并设为开机自启：

```bash
apt update
apt install docker.io -y
systemctl start docker
systemctl enable docker
```

## 三、创建数据目录

机器人的数据库、配置、学习到的广告规则都放在这个目录里，重启或升级都不会丢：

```bash
mkdir -p /root/tg-bot-data
```

## 四、配置 .env

这一步最关键。先在数据目录里建 `.env`，把真实值填进去。下面这份是最小可用配置，先跑起来再说：

```bash
nano /root/tg-bot-data/.env
```

把下面内容粘进去，只改等号右边的值。注意所有标点必须是英文半角，`#` 注释要独占一行：

```env
BOT_TOKEN=你的机器人Token
ADMIN_ID=你的数字ID
BOT_DB_PATH=/app/data/bot_core.db
```

保存后收紧权限，避免别人读到 token：

```bash
chmod 600 /root/tg-bot-data/.env
```

## 五、启动机器人

拉镜像并用 `--env-file` 挂上 `.env` 启动：

```bash
docker pull ghcr.io/sykin7/spamguard-bot-v3:latest
docker run -d --name tg-bot --restart always \
  --env-file /root/tg-bot-data/.env \
  -v /root/tg-bot-data:/app/data \
  --log-opt max-size=50m --log-opt max-file=3 \
  ghcr.io/sykin7/spamguard-bot-v3:latest
```

参数含义：

- `-d`：后台运行，关掉 SSH 也不掉线。
- `--restart always`：VPS 重启或程序崩溃后自动拉起。
- `--env-file`：从 `.env` 读取所有配置。
- `-v`：把容器里的 `/app/data` 映射到 VPS 的 `/root/tg-bot-data`，数据持久化。
- `--log-opt`：限制日志大小，单文件最大 50MB、最多 3 个，防止日志把小硬盘撑爆。

## 六、确认启动成功

看最近日志：

```bash
docker logs --tail=20 tg-bot
```

看到 `Bot Started.` 就说明成功了。这时给机器人私聊发个 `/start`，能收到菜单就正常。

## 七、改了 .env 之后怎么重建

**重点：`docker restart` 不会重新读取 `--env-file`。** 只要动了 `.env`，就必须删掉容器重建，配置才会生效：

```bash
docker rm -f tg-bot
docker run -d --name tg-bot --restart always \
  --env-file /root/tg-bot-data/.env \
  -v /root/tg-bot-data:/app/data \
  --log-opt max-size=50m --log-opt max-file=3 \
  ghcr.io/sykin7/spamguard-bot-v3:latest
```

数据都在 `/root/tg-bot-data` 里，删容器不会丢数据。重建后再看一次日志确认 `Bot Started.`。

## 八、升级到新版本

仓库有更新、镜像重新构建后，这样升级：

```bash
docker pull ghcr.io/sykin7/spamguard-bot-v3:latest
docker rm -f tg-bot
docker run -d --name tg-bot --restart always \
  --env-file /root/tg-bot-data/.env \
  -v /root/tg-bot-data:/app/data \
  --log-opt max-size=50m --log-opt max-file=3 \
  ghcr.io/sykin7/spamguard-bot-v3:latest
```

## 九、常用命令

私聊机器人（只有 `ADMIN_ID` 本人能用管理命令）：

- `/status`：查看机器人状态。
- `/reloadrules`：重新加载广告规则。
- `/vlist wl|bl|ban`：查看白名单 / 黑名单 / 封禁名单。
- `/awl ID`、`/dwl ID`：加入 / 移出白名单。
- `/abl ID`、`/dbl ID`：加入 / 移出黑名单。
- `/unban ID`：解除临时封禁。
- `/resetverify`：清空所有人的验证状态，全部重新验证。
- `/gb 内容`：向所有已知用户广播。
- `/spamtest 内容`：测试一段文字会不会被判成广告，会强制调一次 AI 显示结果。

群内（普通成员可用）：

- `/id`：查看自己的 Telegram 数字 ID。
- `/help`：查看群内可用命令。
- `/spamtest 内容`：测试一段文字会不会被判成广告，不会真的封禁。

群内（群管理员可用，只作用于当前群）：

- `/status`：查看机器人状态。
- `/reloadrules`：重新加载广告规则。
- `/ban ID`：封禁指定用户，同时写入全局黑名单，之后该用户申请加入任何已接管的群都会被自动拒绝并封禁；也可以回复对方消息后发送 `/ban`。
- `/unban ID`：解除该用户在当前群的封禁。只有当这条全局黑名单是本群封进去的、并且该用户没有被其他群封禁时，才会同时移出全局黑名单；否则只解除本群，全局仍然保留。

群内自动永久封禁（发广告被判定命中）同样会写入全局黑名单。全局白名单里的用户，群管理员的 `/ban` 封不动，会提示需要最高管理员处理。

全局白名单用户在群里也免检：发言不做广告判定，入群申请直接通过，不查频道关注。

最高管理员 `ADMIN_ID` 不受上面的限制：在群里执行 `/unban` 一定会同时清掉全局黑名单，也可以在私聊用 `/abl ID` 加黑、`/dbl ID` 移出。

## 十、配置项说明

`.env` 支持的全部配置见项目里的 `.env.example`，都有中文注释。这里挑最常用的解释。

### 验证码

- `CAPTCHA_TEXT_FALLBACK`：默认 `false`，用户必须点按钮验证；设 `true` 允许直接发数字答案。生产环境建议保持 `false`。

### 自定义提示语（可选，留空用默认）

- `WELCOME_ZH` / `WELCOME_EN`：中 / 英文欢迎语。
- `VERIFIED_ZH` / `VERIFIED_EN`：验证通过提示。
- `AUTO_REPLY_ZH` / `AUTO_REPLY_EN`：自动回复提示。

### 第三方 AI 广告识别（可选）

不配这些时机器人只用本地规则，功能不受影响。想开 AI 就填：

- `AI_ENABLED`：设 `true` 才启用。
- `AI_PROVIDER`：`openai-compatible`（默认，兼容绝大多数网关）、`anthropic`、`gemini` 三选一。
- `AI_BASE_URL`：API 地址，按服务商填。
- `AI_API_KEY`：模型密钥，只填在 VPS 本机。
- `AI_MODEL`：模型名，例如 `gpt-4o-mini`、`deepseek-chat`。
- `AI_TIMEOUT`：请求超时秒数，聚合网关首字慢建议 `45`。
- `AI_MIN_SCORE`：本地风险分达到这个值才触发 AI 复核，默认 `5`，调低更爱叫 AI，调高更省。
- `AI_ALWAYS_CHECK`：`true` 每条都问 AI（1C1G 扛不住，仅测试用），`false` 只有可疑消息才问，日常用 `false`。
- `AI_PROFILE_CHECK`：`true` 时入群申请也用 AI 判定用户资料。

**AI 什么时候会真正调用**：必须同时满足三条 —— `AI_ENABLED=true`、本地关键词没直接拦下、这条消息风险分 ≥ `AI_MIN_SCORE`。明显广告本地直接封不叫 AI，完全干净的消息直接放行也不叫 AI，只有卡在中间的可疑消息才交给 AI，这样最省资源。AI 判定成功时不打日志，只有失败（超时 / 解析失败）才打，所以日志干净代表 AI 正常。

### 群聊管理（可选）

机器人必须被设为目标群的管理员，才能审核入群、删消息、封人。

- `GROUP_ENABLED`：是否启用群管理与入群审核。
- `GROUP_JOIN_APPROVE`：是否接管入群申请（群需开“需要审核”）。
- `GROUP_AUTO_APPROVE`：正常申请是否自动通过，`false` 时只通知管理员由人工决定。
- `GROUP_JOIN_REVIEW_TIMEOUT`：人工审核超时秒数，默认 600。超时后按规则兜底：判为广告的自动拒绝并在该群封禁，正常的自动通过。
- `GROUP_JOIN_REQUIRED_CHANNEL`：可选，入群前必须先关注的频道，填 `@频道用户名` 或数字 ID，留空关闭。机器人需要先加入该频道才能检查关注状态。
- `GROUP_BAN_ON_SPAM`：群内发广告是否永久封禁该用户，默认 `true`。
- `GROUP_DELETE_SPAM`：是否删除广告原消息。
- `GROUP_IDS`：只管理这些群，逗号分隔数字群 ID，留空表示管理所有已启用群。
- `GROUP_ADMIN_IDS`：额外的协管数字 ID，逗号分隔，留空则只用 `ADMIN_ID`。Telegram 群原生管理员会自动识别，只能处理本群审核。

`ADMIN_ID` 是最高权限所有者，独享私聊双向转发、广播、全局设置；`GROUP_ADMIN_IDS` 里的人只能管群内审核，碰不到全局权限。

### 群内 AI 闲聊（可选）

群成员 @ 机器人用户名，或直接回复机器人的消息，AI 会用纯文本回话。需要 `AI_ENABLED=true` 且 `GROUP_AI_CHAT=true` 一起开。

- `GROUP_AI_CHAT`：是否开启，默认 `false`。
- `GROUP_AI_CHAT_COOLDOWN`：同一用户两次聊天的最小间隔秒数，默认 15。
- `GROUP_AI_CHAT_MAX_CHARS`：回复最大字符数，默认 500，超出截断。
- `GROUP_AI_CHAT_MAX_PER_MIN`：全群合计每分钟最多几次闲聊，默认 10，先于个人冷却生效。
- `AI_CHAT_PERSONA`：人物设定，留空用默认小助手人格，只改说话风格。

安全边界：广告检测永远先跑，命中广告的消息直接删除或封禁，不会进入聊天。只把用户这句话的文本发给模型，不发服务器路径、环境变量、数据库内容。模型没有任何工具和权限，只能回文本，改不了黑白名单也执行不了命令。以 `/` 开头的命令不进入聊天。全局黑名单用户不会得到回复。凭据复用现有 `AI_BASE_URL` / `AI_API_KEY` / `AI_MODEL`，不需要第二套。

人物设定分两层：`AI_CHAT_PERSONA` 管说话风格，随便改；底层七条行为规则由代码强制附加，改人格改不掉（禁执行命令、禁暴露密钥配置源码、禁改黑白名单、禁切换身份和开发者模式、禁发广告联系方式、禁编造运行环境、限三句话内）。

误封说明：`@` 机器人自己不算广告触达信号，评分前会被剥掉，正常提问不会因为 `@` 多加 2 分而被误判。但 `@` 着机器人发广告照样删照样封，`@` 只决定要不要回话，不影响广告检测。

### 广告规则学习与同步（可选）

机器人拦下广告后会把稳定特征写进本地 SQLite，重复命中达到阈值自动确认学习。不配下面的同步项时只存本地，不推送任何远程文件。

- `RULE_LEARN_ENABLED` / `RULE_AUTO_LEARN_ENABLED`：是否开启学习 / 自动学习。
- `RULE_AUTO_LEARN_THRESHOLD`：同一特征命中几次后自动学习，默认 3。
- GitHub 同步：`RULE_SYNC_GITHUB_REPO`、`RULE_SYNC_GITHUB_PATH`、`RULE_SYNC_GITHUB_BRANCH`、`RULE_SYNC_GITHUB_TOKEN`。
- R2 同步：`RULE_SYNC_R2_ENDPOINT`、`RULE_SYNC_R2_BUCKET`、`RULE_SYNC_R2_KEY`、`RULE_SYNC_R2_ACCESS_KEY`、`RULE_SYNC_R2_SECRET_KEY`。

R2 相关的用量控制、每 3 小时同步、多账户自动切换、9GB 硬上限等细节都在 `.env.example` 里有完整中文注释，按需开启即可。日常判定和学习都走本地，不会每条消息都请求 R2。

## 十一、常见问题

- **改完 `.env` 没生效**：忘了重建容器，`restart` 不重读配置，按第七节重建。
- **AI 一直超时**：网关首字慢，把 `AI_TIMEOUT` 调到 `45` 再重建。
- **AI 好像没反应**：AI 成功时不打日志，用 `/spamtest 一段可疑文字` 直接看结果；或临时把 `AI_ALWAYS_CHECK=true` 测完再改回 `false`。
- **数据会不会丢**：所有数据在 `/root/tg-bot-data`，删容器、升级都不影响，除非你手动删这个目录。
- **没配 GitHub / R2 会不会报错**：不会，留空就是纯本地模式，功能不受影响。
