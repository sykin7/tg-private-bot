 #  增加OCR识图屏蔽广告  #

主要改好的点：

- `IMAGE_OCR_ENABLED=true` 后，非白名单用户发图片必须先通过 OCR/AI 识别；识别失败、配置缺失、接口超时、下载失败都不会转发给管理员。
- 图片超过 `IMAGE_OCR_MAX_BYTES`，默认 20MB，会提示用户压缩后再发，提示受 `IMAGE_NOTICE_COOLDOWN` 限频。
- 用户 30 秒内重复发图片，会提示“请等待 xx 秒后再发送图片”，也受提示限频控制。
- OCR 锁改成阻塞式单队列处理，同一进程内图片会一张一张排队识别，不再因为“忙”就跳过。
- 管理员会收到 OCR 问题原因，例如 `missing_api_key`、`missing_api_url`、`missing_model`、`ocrspace_timeout`、`ai_http_429`、`telegram_download_failed`。
- 第三方 AI/中转站已适配 `IMAGE_OCR_API_URL + IMAGE_OCR_API_KEY + IMAGE_OCR_MODEL`，不是只靠 key。
- AI 识别单独加了 `IMAGE_AI_OCR_MAX_BYTES`，默认 5MB，避免 base64 请求太大拖垮服务器或被中转站拒绝。
- 删除了旧的 `_legacy_enforce_photo_ocr_gate`，避免以后误用旧逻辑导致配置缺失还放行。
- AI 默认提示词加强了广告识别倾向，会重点提取二维码、联系方式、用户名、URL、支付、虚拟币等广告线索。


现在推荐配置第三方 AI 中转大概是这样：

```env
IMAGE_OCR_ENABLED=true
IMAGE_OCR_PROVIDER=openai_compatible
IMAGE_OCR_API_URL=https://你的中转站/v1/chat/completions
IMAGE_OCR_API_KEY=你的key
IMAGE_OCR_MODEL=你的视觉模型名
IMAGE_OCR_MAX_BYTES=20971520
IMAGE_AI_OCR_MAX_BYTES=5242880
IMAGE_COOLDOWN_SECONDS=30
IMAGE_NOTICE_COOLDOWN=30
IMAGE_OCR_TIMEOUT=10
```

OCR.space 也还能用：

```env
IMAGE_OCR_ENABLED=true
IMAGE_OCR_PROVIDER=ocrspace
IMAGE_OCR_API_KEY=你的OCR_SPACE_KEY
```

整体现在是偏“宁可拦住，也不漏广告图给管理员”的策略。
